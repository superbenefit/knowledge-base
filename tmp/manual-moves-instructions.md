# Gate 1: Manual Moves (Obsidian)

## 1a. Move `notes/archive/` → `archive/`
✅ Done

## 1b. Rename `notes/` → `docs/`
✅ Done

## 1c. Create `data/` tree
✅ Done via CLI

## 1d. Move `tags/*` → `data/concepts/`
✅ Done

## 1e. Move `links/*` → `data/links/`
✅ Done

---

## 1f. Move `artifacts/primitives-framework/` → `docs/primitives-framework/`
Right-click `artifacts/primitives-framework/` → Move folder to → `docs/`

## 1g. Move `artifacts/reimagining-power/` → `docs/reimagining-power/`
Right-click `artifacts/reimagining-power/` → Move folder to → `docs/`

## 1h. Sort `artifacts/misc/` by type

| Source folder | Destination |
|---------------|-------------|
| `artifacts/misc/articles/*` | `data/stories/articles/` |
| `artifacts/misc/patterns/*` | `data/resources/patterns/` |
| `artifacts/misc/guides/*` | `data/resources/practices/` |
| `artifacts/misc/playbooks/*` | `data/resources/playbooks/` |
| `artifacts/misc/stories/*` | `data/stories/studies/` |

Move each subfolder's contents (not the folder itself) to the destination.

## 1i. Delete empty `artifacts/`
After moves complete, delete `artifacts/index.md`, `artifacts/readme.md`, then delete empty folder.
